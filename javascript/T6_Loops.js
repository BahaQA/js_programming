
/**
 * Basic loops (for, while, do...while) are the same in Java and JavaScript.
JavaScript has additional looping constructs (for...in, for...of), and Java has an enhanced for loop for collections.
 */

for (let i = 0; i < array.length; i++) {
    const element = array[i];
    array.forEach(element => {
        
    });
}